servers:
- port:
    number: 80
    protocol: http2
  hosts:
  - example.com
- port:
    number: 443
    protocol: http2
  hosts:
  - example.com
  tls:
    mode: SIMPLE
    serverCertificate: /etc/certs/server.pem
    privateKey: /etc/certs/key.pem
